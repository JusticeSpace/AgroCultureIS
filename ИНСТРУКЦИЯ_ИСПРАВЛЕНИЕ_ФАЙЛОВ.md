# 🔧 Ручное исправление файлов Entity Framework

Если `git reset --hard` не помог, исправь эти файлы вручную в Visual Studio:

---

## 📝 Файл 1: AgroCulture/Cabins.cs

**Найди строки 25-33 и замени на:**

```csharp
    public int CabinId { get; set; }
    public string Name { get; set; }
    public string Description { get; set; }
    public int MaxGuests { get; set; }           // ⬅️ БЫЛО Capacity
    public decimal PricePerNight { get; set; }
    public string ImageUrl { get; set; }
    public bool IsActive { get; set; }           // ⬅️ УБРАЛИ IsAvailable и CreatedAt
```

**Найди строки 38-40 (CabinAmenities) и УДАЛИ CabinAmenities1:**

```csharp
    [System.Diagnostics.CodeAnalysis.SuppressMessage("Microsoft.Usage", "CA2227:CollectionPropertiesShouldBeReadOnly")]
    public virtual ICollection<CabinAmenities> CabinAmenities { get; set; }
    // ⬅️ УДАЛИ CabinAmenities1 если есть
```

**В конструкторе (строки 18-23) УДАЛИ CabinAmenities1:**

```csharp
    public Cabins()
    {
        this.Bookings = new HashSet<Bookings>();
        this.CabinAmenities = new HashSet<CabinAmenities>();
        // ⬅️ УДАЛИ CabinAmenities1 если есть
    }
```

---

## 📝 Файл 2: AgroCulture/BookingsDetails.cs

**Найди строку 27 и замени:**

```csharp
// БЫЛО:
public int Capacity { get; set; }

// СТАЛО:
public int MaxGuests { get; set; }
```

---

## 📝 Файл 3: Проверь что существует AgroCulture/CabinAmenities.cs

Если файла **НЕТ**, создай его с таким содержимым:

```csharp
//------------------------------------------------------------------------------
// <auto-generated>
//     Этот код создан по шаблону.
// </auto-generated>
//------------------------------------------------------------------------------

namespace AgroCulture
{
    using System;
    using System.Collections.Generic;

    public partial class CabinAmenities
    {
        public int CabinId { get; set; }
        public int AmenityId { get; set; }

        public virtual Amenities Amenities { get; set; }
        public virtual Cabins Cabins { get; set; }
    }
}
```

Затем **добавь** его в проект:
- ПКМ на проект → Add → Existing Item → выбери CabinAmenities.cs

---

## ✅ После всех изменений:

1. Сохрани все файлы (Ctrl+Shift+S)
2. Rebuild Solution (Ctrl+Shift+B)
3. Проект должен собраться БЕЗ ошибок! 🎉

---

## 🎓 Что исправили:

- ✅ `Capacity` → `MaxGuests` (новая БД использует MaxGuests)
- ✅ Удалены `IsAvailable` и `CreatedAt` из Cabins (их нет в новой БД)
- ✅ Убрана лишняя коллекция `CabinAmenities1`
- ✅ Добавлен класс `CabinAmenities` если отсутствовал
